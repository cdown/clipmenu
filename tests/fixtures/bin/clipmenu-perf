#!/usr/bin/env bash

exec {BASH_XTRACEFD}> >(tee "${log?}" |
                 sed -u 's/^.*$/now/' |
                 date -f - +%s.%N > "${tim?}")

export BASH_XTRACEFD

set -x

dmenu() { :; }
xsel() { :; }

# shellcheck disable=SC1091
source clipmenu "$@"
